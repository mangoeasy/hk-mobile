$('.owl-carousel').owlCarousel({
  loop: true,
  margin: 0,
  items: 1,
  autoplay: true,
  autoplay: 2000
})
$('body').flowtype({
  minimum: 320,
  maximum: 640,
  minFont: 12,
  maxFont: 35,
  fontRatio: 30

});

$('.count-board').scrollToFixed();
